# Claude Code Project Instructions

## Commands

- Install dependencies in this deck with `npm install`.
- Run this demo with `npm run demo:3.2`.
- Build the deck with `npm run build`.

## Workflow

- Explore before editing when the task touches multiple files.
- Prefer focused tests over the full suite while iterating.
- At completion, report modified files, verification commands, and residual risks.

## Imports

If this repository already has shared agent rules, import them explicitly:

```md
@AGENTS.md
```
