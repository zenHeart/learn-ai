---
layout: section
---

# 2. Context 工程

---

# 什么是 Context Engineering？

<v-clicks>

1. **Context**：模型本轮推理时**实际看到的全部输入 Token**。

2. **Context Engineering**：围绕任务目标，选择、检索、压缩、排序和维护这些 Token，让模型看到**最小但足够的高信号信息**。

</v-clicks>

<v-click>

```
Context = System Prompt
        + Memory 注入（CLAUDE.md / `.cursor/rules/`）
        + RAG 召回内容  ← 向量检索：把相关文档找出来注入窗口
        + 对话历史
        + 工具调用结果
        + 当前 User 输入（你写的 Prompt）
```

</v-click>

<v-click>

> **本质**：Prompt 定义任务契约，Context 提供事实材料；Context Engineering 解决的是**给什么、给多少、放哪里、何时更新**。

</v-click>

---

# 为什么需要 Context Engineering？


<v-click>

| 约束 | 含义 | 工程后果 |
|------|------|---------|
| **不在权重的事实** | 模型不知道最新代码、业务状态、实时信息 | 必须注入当前事实与证据 |
| **窗口限制** | 容量、成本、注意力都有限 | 必须做最小有效集 |
| **[上下文腐烂](https://www.trychroma.com/research/context-rot)** | 上下文越长，模型越不稳定地使用信息，噪声会放大退化 | 不能把“塞更多”当成“更可靠” |
| **位置会影响注意力** | Lost in the Middle 现象 | 关键事实要分块、排序、放到高权重位置 |

</v-click>

---

# Context Window 的完整结构

<ContextWindowVisualizer />

<!-- 演讲者备注：点击逐层出现：System Prompt、Memory/Project Rules、RAG/引用文件、Conversation History、Tool Results、Current User Prompt。重点强调 Context 不是资料堆，而是每轮由 Harness 或工具链动态组装出来的输入窗口。 -->

---

# 首尾定律：Attention 是位置敏感的

> 引用：[Liu et al. 2023, *Lost in the Middle](https://arxiv.org/abs/2307.03372) 文档位置直接影响模型注意力权重


<v-click>

<div class="attention-barchart">
  <div class="attention-barchart__bars">
    <div class="attention-barchart__bar">
      <div class="attention-barchart__fill high" style="height: 140px;"></div>
      <div class="attention-barchart__label">
        <strong>开头</strong>
        高权重<br />
        System Prompt / 规则 / 约束
      </div>
    </div>
    <div class="attention-barchart__bar">
      <div class="attention-barchart__fill low" style="height: 78px;"></div>
      <div class="attention-barchart__label">
        <strong>中间</strong>
        低权重<br />
        Context 中段 / 背景材料 / RAG / 历史消息
      </div>
    </div>
    <div class="attention-barchart__bar">
      <div class="attention-barchart__fill high" style="height: 140px;"></div>
      <div class="attention-barchart__label">
        <strong>结尾</strong>
        高权重<br />
        User Task / 当前问题 / 最后约束
      </div>
    </div>
  </div>
  <div class="attention-barchart__note">这是位置偏好的示意图，不是精确测量曲线。</div>
</div>

</v-click>

<v-clicks>

- **首尾定律**：关键约束 → System Prompt（开头）；当前任务 → User Input（结尾）
- 中间内容会被"淹没"——**无关信息坚决过滤**

</v-clicks>

<v-click>

> **工程原则**：宁可短而精，不要长而散；位置即权重。长任务多用 `/clear, /compact` 压缩上下文渐进式完成。

</v-click>


---
layout: two-cols-header
---

# Context 实战原则

**来自 Cursor / Claude Code / Manus 的共同经验**：Context 管理不是把材料堆满，而是让 Agent 在工作中持续取证。

::left::

<div class="col-good">

### ✅ 该固定注入的

- 项目规则：`AGENTS.md` / `CLAUDE.md` / Rules
- 当前状态：cwd、git status、最近打开文件
- 任务目标：当前用户请求和验收标准

### ✅ 该运行时读取的

- 大文件、日志、网页、PDF、历史对话
- 通过 `Read` / `Grep` / `@file` / RAG 按需拉取

</div>

::right::

<div class="col-good">

### ✅ 该外部化保存的

```text
todo.md   # 当前计划
docs      # 项目文档规约
README.md # 项目地图，总览
```

- 长任务反复更新 todo，让目标回到最近上下文
- 错误观察保留摘要，避免重复踩同一个坑

</div>

---
layout: two-cols-header
---

# Context 安全红线


<v-click>

| 风险 | 攻击形态 | 防御 |
|------|---------|------|
| **数据泄漏** | `.env`、密钥、内部文档被读入 Context → 模型复述/记入历史 | `.cursorignore` / `.gitignore` 黑名单 + 提交前扫描 |
| **Context 污染** | 过期文档、错误注释混入 → AI 误判 | 仓库即文档 + 定期清理 + 版本标记 |
| **Token 失控** | 盲目堆 @全目录 → 成本爆炸 + Lost in the Middle | 最小有效集原则 + 监控 Token 消耗 |

</v-click>

---

# 工具复用：不同工具的 Context 注入形态

<v-clicks>

**Context 注入不是手动拼字符串**——主流工具各有自己的"注入语法"：

</v-clicks>

<v-click>

| 工具 | Context 注入语法 | 自动注入源 |
|------|----------------|----------|
| **Cursor** | `@file` `@code` `@docs` `@web` `@git` | `.cursor/rules/`  |
| **Claude Code** | `@path` + `Read` tool + Skill，`/add-dir、/clear、/compact`  | `CLAUDE.md` + `~/.claude/CLAUDE.md` |
| **通用 MCP**（Model Context Protocol）| 标准化协议 → 可插拔 Context 源 | MCP Server 配置 |

</v-click>

---
layout: center
---

# 本章小结：Context 工程

<v-clicks>

1. **信息边界**：Context 是模型本轮真正看见的世界，没进窗口的信息等于不存在, 模型无法完成任务优先考虑信息给的足不足

2. **注意力预算**：精准比全面重要，最小有效集同时降低噪声、成本和 Lost in the Middle 风险

3. **动态装配**：按任务检索、压缩、排序和标注来源，把关键事实放到正确位置，并保留恢复路径

</v-clicks>

<!--
演讲者备注：Context 工程的核心是"给得准"。不要把它讲成资料越多越好，而要强调信息边界、注意力预算和动态装配。首尾定律是最重要的机制，安全部分要落到最小注入和可追溯来源。
-->
