---
layout: section
---

# 6. QA 问答环节

---
layout: default
---
### **Q1: 为什么我写的 Skill，AI 经常乱用或者根本不听我的规则？**

A: **因为你把它当成了人，而不是当成了编译器。**

<div class="mt-6 space-y-5 text-sm">

- **官方铁律**: 根据 [Anthropic 的 Tool Use 最佳实践](https://docs.anthropic.com/en/docs/build-with-claude/tool-use)，不要对 AI 说“请尽量避免使用...”，而是要说“**绝对禁止**在 XX 情况下使用，否则会导致严重错误”。
- **Schema 约束**: 把所有能用 `enum` (枚举) 框死的地方全部框死。如果你让 AI 传一个自由的字符串，它就会产生幻觉。给它明确的下拉菜单，它就不会出错。

</div>

---
layout: two-cols
---

### **Q2: `.cursorrules` (规则) 和 CLI 里的 Skill 到底有什么区别？**

A: **触发机制和作用范围不同。**

<div class="mt-6 space-y-4 text-sm">

1. **`.cursorrules` (被动守则)**: 只要在这个项目里写代码，它就会作为 Background Context 一直存在，默默约束 AI 的所有行为。
2. **Skill (主动大招)**: 是一套包含具体步骤的“工作流”。它平时潜水，只有当你敲下 `/audit` 或者 `/deploy` 时才被唤醒，去执行一套复杂的 SOP。

</div>

::right::

### **Q3: 遇到复杂的流水线，我是写一个大 Skill，还是写多个小 Skill？**

A: **坚决写多个小 Skill。**

<div class="mt-6 space-y-4 text-sm">

1. **控制变量**: 像写微服务一样写 Skill。一个叫 `run-linter`，一个叫 `build-image`。
2. **编排机制**: 让一个高等级的 Agent (比如接下来我们要讲的第五期核心内容) 去根据当前状态，按需调用这些原子化的 Skill。把它们揉成一团只会导致 AI 经常在中间某一步因为超时而全盘崩溃。

</div>

---
layout: center
---

# 🎉 Thank You

<div class="text-center mt-12">

<h2 class="text-2xl font-bold mb-8">Skill: 把“团队潜规则”变成“AI 的思想钢印”</h2>

<div class="inline-block p-8 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700/50 rounded-2xl shadow-sm">
  <div class="text-xl flex items-center justify-center gap-4 font-semibold text-slate-700 dark:text-slate-200">
    <span class="px-4 py-2 bg-white dark:bg-slate-700 rounded-lg shadow-sm">Tool Use 底层能力</span>
    <span class="text-blue-500 font-bold">+</span> 
    <span class="px-4 py-2 bg-white dark:bg-slate-700 rounded-lg shadow-sm">严格清晰的 Schema</span>
  </div>
</div>

<div class="mt-16 text-2xl font-bold text-slate-700 dark:text-slate-300">
  Questions? 🙋‍♂️
</div>

</div>
