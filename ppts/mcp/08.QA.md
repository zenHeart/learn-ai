---
layout: section
---

# 8. QA 问答环节

---
layout: default
---

### **Q1: 为什么非要套一层 MCP 增加复杂度？我直接在脚本里调 API 不是更简单吗？**

A: **对于“一次性脚本”，直接调 API 确实更简单；但对于“生态级集成”，MCP 是降维打击。**

<div class="mt-6 space-y-5 text-sm">

- **复利效应**: [Anthropic 在官方博客指出](https://www.anthropic.com/news/model-context-protocol)，只要你写了一次 Jira 的 MCP Server，明天不管是 Cursor、Windsurf、Claude 甚至后天的某个新 IDE 出现，**不需要改一行代码**，所有的 AI 工具都能瞬间具备操作 Jira 的能力。
- **权限收口**: 如果直接让 AI 写 Python 脚本去调数据库，意味着你要把生产环境的账号密码直接暴露给大模型（极度危险）。而 MCP 架构把密钥和具体执行逻辑封装在本地 Server 里，对大模型完全隔离。

</div>

---
layout: two-cols
---

### **Q2: 自己开发 MCP Server 门槛高吗？**

A: **非常低，几乎等同于写一个普通的 Node.js / Python 函数。**

<div class="mt-6 space-y-4 text-sm">

1. **SDK 完善**: 官方提供了 `@modelcontextprotocol/sdk` (TS) 和 `mcp` (Python)。
2. **特别是 Python 的 FastMCP**: 它的心智模型完全借鉴了 FastAPI。你只需要写一个普通的 Python 函数，加一个 `@mcp.tool()` 装饰器，它就自动变成了一个标准的 MCP Tool。

</div>

::right::

### **Q3: MCP 只能用于本地（stdio）吗？可以跨网络调用吗？**

A: **完全可以跨网络。**

<div class="mt-6 space-y-4 text-sm">

1. **企业内网部署**: 你可以把 MCP Server 部署在公司内网的一台服务器上（例如 10.0.0.1），然后使用 **SSE (Server-Sent Events)** 作为 Transport 层暴露。
2. **集中管控**: 全体研发的 Cursor/Claude 都可以通过 HTTP 连接到这个统一的节点。这不仅方便版本控制，还能在服务端统一做权限审计（谁在哪天让 AI 查了哪个表）。

</div>

---
layout: center
---

# 🎉 Thank You

<div class="text-center mt-12">

<h2 class="text-2xl font-bold mb-8">MCP：打通 AI 与私有数据的“最后一公里”</h2>

<div class="inline-block p-8 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700/50 rounded-2xl shadow-sm">
  <div class="text-xl flex items-center justify-center gap-4 font-semibold text-slate-700 dark:text-slate-200">
    <span class="px-4 py-2 bg-white dark:bg-slate-700 rounded-lg shadow-sm">AI 强大的推理</span>
    <span class="text-blue-500 font-bold">+</span> 
    <span class="px-4 py-2 bg-white dark:bg-slate-700 rounded-lg shadow-sm">MCP 丰富的数据源</span>
  </div>
</div>

<div class="mt-16 text-2xl font-bold text-slate-700 dark:text-slate-300">
  Questions? 🙋‍♂️
</div>

</div>
