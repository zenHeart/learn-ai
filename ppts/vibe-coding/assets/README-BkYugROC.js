const n=`# 4.2 feature 需求开发 (PRD to PR)

**一句话核心**：以 PRD.md + UI 设计稿为输入，用 PAIR 工作流驱动 AI **从需求文档到功能 PR** 的端到端交付。

---

## 1. 概念简述

已有项目新增功能是研发最常见的场景。在 Vibe Coding 的 PAIR 工作流中：

- **Plan 阶段**：将 PRD + UI 转化为 \`design.md\`（接口/数据流/组件边界）和 \`todo.md\`（Story 拆解），AI 即可获得结构化执行约束
- **Assess 阶段**：人工 Review \`design.md\`，在首行代码之前确认接口字段完整、组件边界清晰
- **Implement 阶段**：以 Story 为单位原子化执行，每次只消耗一个 Todo
- **Review 阶段**：每个 Story 完成后验收功能，对比 Diff 关注边缘情况

---

## 2. 前置条件

- 已安装 **Node.js 18+** 以及 **Cursor** 或 **Claude Code**
- 操作前请进入 \`ppts/vibe-coding/examples\` 并确保已执行 \`npm install\`
- 准备好 PRD 文档（可用下方示例文件替代）

---

## 3. PAIR 操作步骤

### 🗺️ Plan — 从 PRD 生成技术设计与 Todo

**目标**：生成 \`design.md\` + \`todo.md\`，作为所有执行的基线。

1. 在项目目录准备好输入材料：
   \`\`\`
   feature/
   ├── PRD.md          # 需求文档（功能描述、验收标准）
   └── UI/             # UI 截图或 Figma 链接
   \`\`\`

2. 打开 Cursor / Claude Code，进入 **Plan Mode**，输入 Prompt：

   \`\`\`text
   @PRD.md 和 UI 截图是本次需求的输入。

   请帮我：
   1. 生成 design.md，包含：
      - 涉及的 API 接口（字段、入参、返回值）
      - 数据流向（从 API 到 Store 到组件）
      - 需要新增 / 修改的组件列表及其 Props
      - 与现有模块的边界说明
   2. 生成 todo.md，将需求拆分为独立可执行的 Story，每条一行，按依赖顺序排列

   先输出计划，等我确认后再执行。
   \`\`\`

3. **预期结果**：AI 输出 \`design.md\` + \`todo.md\` 草案，无文件变更

---

### 🔍 Assess — Review design.md，锁定执行基线

**目标**：确认接口/数据流/组件边界无遗漏，锁定后不随意调整。

1. 阅读 \`design.md\`，重点检查：

   | 检查项 | 关注点 |
   |--------|--------|
   | API 接口 | 字段是否完整？是否有缺失的必填参数？ |
   | 数据流 | Store 更新时序是否正确？有无竞态风险？ |
   | 组件边界 | 新组件是否与现有组件职责重叠？ |
   | todo 粒度 | 每条 Story 是否独立可验收？ |

2. 如有偏差，回复修正（示例）：
   \`\`\`text
   design.md 需要调整：
   - /api/user/info 接口缺少 \`avatar\` 字段，请补充
   - UserCard 组件应从现有的 Avatar 组件继承，不要新建
   - todo.md 第 3 条「实现 UserCard」应拆分为：「3a. 实现 Avatar」和「3b. 组合为 UserCard」
   \`\`\`

3. 确认对齐：
   \`\`\`text
   design.md 已确认，请按 todo.md 顺序执行，每完成一条暂停等待验收。
   \`\`\`

4. **预期结果**：\`design.md\` 和 \`todo.md\` 文件落地，执行基线锁定

---

### ⚡ Implement — 按 Story 原子化执行

**目标**：逐条消耗 \`todo.md\`，每次只处理一个 Story。

1. 触发执行（示例，每次只执行一条）：
   \`\`\`text
   @ 引用 design.md 和 AGENTS.md

   执行 todo.md 第 1 条：实现 /api/user/info 接口的 composable 层（useUserInfo.ts）
   要求：
   - 严格按 design.md 中的字段定义
   - 参考 AGENTS.md 中的 composable 规范
   - 包含 loading / error 状态
   \`\`\`

2. 复杂 Story 触发子 PAIR 循环：
   \`\`\`text
   这个 Story 较复杂，先用 Plan Mode 为「实现 UserCard 组件」生成子 design.md 和子 todo，
   然后按子 todo 逐步执行。
   \`\`\`

3. **执行粒度原则**：
   - ✅ 一个 Story = 一个 composable / 一个组件 / 一段数据流
   - ❌ 不要一次让 AI "实现整个 feature"

4. **预期结果**：每步完成后，对应的文件被创建/修改，功能可增量验收

---

### ✅ Review — Story 级验收，保障质量收敛

**目标**：每个 Story 完成后即验收，发现问题立即回溯。

1. 功能验收：
   \`\`\`bash
   npm run test              # 运行相关测试
   npm run type-check        # 检查 TS 类型
   npm run lint              # 检查代码规范
   \`\`\`

2. Diff 审查（在 Cursor / VSCode 中对比变更）：
   - 关注 AI 对**边缘情况**的处理（空数据、网络错误、权限拦截）
   - 确认无多余的 \`console.log\` 或调试代码
   - 确认组件 Props 类型定义完整

3. **回溯触发条件**：
   - 接口字段与 design.md 不符 → 回溯 Plan，修正 design.md
   - 组件边界混乱 → 回溯 Plan，重新拆解 todo.md
   - 出现 TS 错误 → 在当前 Story 内修复（局部 Review），不回溯

4. PR 生成：
   \`\`\`text
   所有 todo 验收通过，请帮我生成 PR 描述，包含：
   - 需求背景（来自 PRD.md 第 X 节）
   - 主要变更（涉及的文件和组件）
   - 边缘情况说明
   - 验收截图（如有）
   \`\`\`

5. **预期结果**：功能测试通过的 PR，描述清晰可 Code Review

---

## 4. 本示例涉及的文件

| 文件/目录 | 说明 |
|-----------|------|
| \`feature/PRD.md\` | Plan 阶段输入：产品需求文档 |
| \`feature/UI/\` | Plan 阶段输入：UI 设计稿截图 |
| \`design.md\` | Plan 阶段产出：技术设计文档 |
| \`todo.md\` | Plan 阶段产出：Story 执行清单 |
| \`4.2.modernization/throttle.ts\` | 遗留代码演示（辅助示例） |
| \`4.2.modernization/throttle.test.ts\` | 验证测试集（辅助示例） |

---

## 5. 核心要点

- **design.md 是执行基线**：一旦 Assess 确认，后续执行严格遵循，减少"AI 自由发挥"的风险
- **Story 粒度决定质量**：Story 越细，上下文越专注，AI 产出质量越高
- **回溯 > 打补丁**：发现 design.md 有误时，直接修正文档并重新执行，而不是在错误基础上用 Prompt 补救

---

## 6. 延伸阅读

- **概念延伸**：对于遗留代码重构，可在 Plan 阶段 @ 引用旧代码 + 新规范（如 \`AGENTS.md\`），生成"重构迁移 design.md"
- **官方文档**：
  - Cursor：[Composer / Agent](https://cursor.com/docs/agent/overview)
  - Claude Code：[大范围重构最佳实践](https://docs.anthropic.com/en/docs/claude-code/best-practices)
`;export{n as default};
