/**
 * 3.2 Rules Matching 示例：本示例主要体验在 Cursor 中打开 Header.tsx / format.ts 并用 Cmd+K 触发规则匹配。
 * 运行本脚本仅作占位；完整步骤见 README。
 */
console.log("3.2 Rules Matching: 请用 Cursor 以 examples 为根目录打开，打开 src/components/Header.tsx 或 src/utils/format.ts，按 Cmd+K 输入 README 中的 Prompt。详见 README。\n");
